//prùhlednost:
div {
    opacity: 0.5;
}

//prùhledost -> zviditelnìní
div {
    opacity: 0.5;
}

    div:hover {
        opacity: 1.0;
    }
//Test
//Test2
//Test4
//container
.container {
    padding: 0;
    border: solid;
    background: none;
    border-block: hidden;
    outline: none;
}

//center container
    .container {
    height: 100px;
    position: relative;
    margin: 0;
    position: absolute;
    top: 50%;
    left: 50%;
    -ms-transform: translate(-50%, -50%);
    transform: translate(-50%, -50%);
}

//opacitybackground -> opacity na celou stránku a tlaèítek
overflow: hidden;
    position: relative;
    position: absolute;
    left: 0;
    top: 0;
    width: 100%;
    height: auto; -> zbyteèné 
 toto staèí ->
 opacity: 0.6;

 //rotace obrázku
  transform: rotate(45deg);

  //zamlžení obrázku
      filter: blur(8px);
    -webkit-filter: blur(8px);

    //jas
    filter: brightness(20%);

    //kontrast
    filter: contrast(220%);
    
    //z myši na ukazováèek
    cursor: pointer;

    //mìli by zmizet hranice
        bordern:none;
        outline:none;



   - najít how to change border around button css!!